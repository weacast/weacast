import{_ as e,c as t,o,ak as a}from"./chunks/framework.B3JW-Vb0.js";const r="/weacast/assets/forecast-data-model.DrscGb0H.svg",i="/weacast/assets/element-data-model.D-nXVT2F.svg",l="/weacast/assets/probe-data-model.Dfr9m2Q4.svg",s="/weacast/assets/probe-result-data-model.B-BLpde4.svg",n="/weacast/assets/alert-data-model.CqH0_Y3K.svg",v=JSON.parse('{"title":"Data model-oriented view of the architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/data-model-view.md","filePath":"architecture/data-model-view.md"}'),d={name:"architecture/data-model-view.md"},c=a('<h1 id="data-model-oriented-view-of-the-architecture" tabindex="-1">Data model-oriented view of the architecture <a class="header-anchor" href="#data-model-oriented-view-of-the-architecture" aria-label="Permalink to &quot;Data model-oriented view of the architecture&quot;">​</a></h1><p>According to the <a href="https://docs.feathersjs.com/guides/about/philosophy.html" target="_blank" rel="noreferrer">Feathers philosophy</a> each data model is manipulated using a <a href="https://docs.feathersjs.com/api/services.html" target="_blank" rel="noreferrer">service interface</a> to perform <a href="https://en.wikipedia.org/wiki/Create,_read,_update_and_delete" target="_blank" rel="noreferrer">CRUD operations</a> of the <a href="https://docs.feathersjs.com/api/databases/common.html" target="_blank" rel="noreferrer">persistence layer</a>. So this data model-oriented view is a service-oriented view in the same manner.</p><p>Because data models internally rely on <a href="http://www.json.org/" target="_blank" rel="noreferrer">JSON</a> they are by nature hierarchical. In the following diagrams each nested JSON object is represented as a smaller <em>bubble</em> in a bigger <em>bubble</em> (the nesting/parent object), the data model instance being the root JSON object or the <em>biggest</em> bubble. The name of the bubble is the name of the nesting object property owing the nested object.</p><h2 id="forecast-data-model" tabindex="-1">Forecast data model <a class="header-anchor" href="#forecast-data-model" aria-label="Permalink to &quot;Forecast data model&quot;">​</a></h2><p>The common properties of a forecast model instance are described by the following data model:</p><p><img src="'+r+'" alt="Forecast data model"></p><p>This data model is instantiated from the <a href="./../guides/basics.html#configuring">configuration</a> and manipulated through the <a href="./../api/forecast.html">Forecast model API</a>.</p><p>The details of each property are the following:</p><ul><li><strong>name</strong> : internal model name that will be used in path to reach the underlying service</li><li><strong>label</strong> : the label for the model in the user interface</li><li><strong>description</strong> : the description for the model in the user interface</li><li><strong>attribution</strong> : the attribution of the data to be displayed when visualized</li><li><strong>model</strong> : the model plugin to be used for this forecast</li><li><strong>bounds</strong> : the geographical bounds covered by the model as an array of decimal values <code>[min longitude, min latitude, max longitude, max latitude]</code></li><li><strong>origin</strong> : the geographical origin of the data grid as an array of decimal values <code>[longitude origin, latitude origin]</code></li><li><strong>size</strong> : the size of the data grid as an array of integer values <code>[width, height]</code></li><li><strong>resolution</strong> : the geographical resolution of the data grid as an array of decimal values <code>[longitude resolution, latitude resolution]</code></li><li><strong>tileResolution</strong> : the geographical resolution of the data tiles as an array of decimal values <code>[longitude resolution, latitude resolution]</code>, if not given will not produce tiles</li><li><strong>timeseries</strong> : if true will also generate tiles with aggregated data over all forecast times</li><li><strong>runInterval</strong> : the period between two forecast runs (in seconds) - it also defines at which hours of the day the forecast data are produced (e.g. <code>00:00 - 06:00 - 12:00 - 18:00</code> for a <code>3600</code> seconds period)</li><li><strong>oldestRunInterval</strong> : how long do the server go back in time when harvesting forecast data (in seconds)</li><li><strong>keepPastForecasts</strong> : by default the server will not gather forecast data older than the curent time, set this to <code>true</code> will you need to keep track of all forecast data even if it refers to past data</li><li><strong>interval</strong> : the period between two forecast time steps (in seconds)</li><li><strong>lowerLimit</strong> : the offset from run time for the first forecast time step to be gathered (in seconds)</li><li><strong>upperLimit</strong> : the offset from run time for the last forecast time step to be gathered (in seconds)</li><li><strong>updateInterval</strong> : the check period for data updates (default is every 15 minutes) <ul><li>if <code>&lt; 0</code> no update will be performed, you need to trigger it manually by calling <code>updateForecastData()</code> on the associated element service</li><li>if <code>= 0</code> a single update will be performed when launching the server</li><li>if <code>&gt; 0</code> an update every N seconds will be performed as long as the server runs</li></ul></li><li><strong>elements</strong> : an array of forecast elements that will be managed for this model, the common properties are the following <ul><li><strong>name</strong> : internal element name that will be used in path to reach the underlying service</li><li><strong>bucket</strong> : download bucket ID</li><li><strong>dataStore</strong> : data store to be used for the forecast data, could be <ul><li><code>db</code> to store it directly in database (default if not given)</li><li><code>fs</code> to store it on the file system</li><li><code>gridfs</code> to store it using <a href="https://docs.mongodb.com/manual/core/gridfs/" target="_blank" rel="noreferrer">GrigFS</a></li></ul></li><li><strong>interval</strong>, <strong>lowerLimit</strong>, <strong>upperLimit</strong> to override on a per-element basis the default forecast options (see description before)</li></ul></li></ul><blockquote><p>By default all elements of a forecast model are downloaded sequentially unless you specify some buckets. If so, buckets are downloaded sequentially but all elements belonging to the same bucket will be downloaded in parallel.</p></blockquote><h2 id="element-data-model" tabindex="-1">Element data model <a class="header-anchor" href="#element-data-model" aria-label="Permalink to &quot;Element data model&quot;">​</a></h2><p>The common properties of a forecast element are described by the following data model:</p><p><img src="'+i+'" alt="Element data model"></p><p>This data model is instantiated when forecast data are gathered by <a href="./../api/plugin.html">Forecast model plugins</a> and manipulated through the <a href="./../api/element.html">Forecast element API</a>.</p><p>The details of each property are the following:</p><ul><li><strong>runTime</strong>: the date/time of the model run that produced the forecast data</li><li><strong>forecastTime</strong> : the date/time of the forecast data</li><li><strong>minValue</strong> : the minimum value of the element in the forecast data</li><li><strong>maxValue</strong> : the maximum value of the element in the forecast data</li><li><strong>data</strong> : the forecast data for the element</li></ul><p>A forecast element will cover the period until the start of the next available forecast, what is called the Time To Live (or TTL in short), this is typically one or three hour(s) for much of forecast data. Once its validity has expired it is automatically removed from the database.</p><p>If the data store of the element in the forecast model configuration is the database then the <strong>data</strong> property containing forecast data is stored in the model, otherwise if the data store is the file system it has an additional <strong>convertedFilePath</strong> (respectively <strong>filePath</strong>) property only used internally to retrieve the JSON converted (respectively source) data on-the-fly.</p><blockquote><p>When tiling is configured each forecast element generate an additional tile set with the following additional properties:</p></blockquote><ul><li><strong>x/y</strong> : coordinate of the tile in the tile set grid</li><li><strong>bounds</strong> : the geographical bounds covered by the tile as an array of decimal values <code>[min longitude, min latitude, max longitude, max latitude]</code></li><li><strong>origin</strong> : the geographical origin of the tile data grid as an array of decimal values <code>[longitude origin, latitude origin]</code></li><li><strong>size</strong> : the size of the tile data grid as an array of integer values <code>[width, height]</code></li><li><strong>resolution</strong> : the geographical resolution of the tile data grid as an array of decimal values <code>[longitude resolution, latitude resolution]</code></li><li><strong>geometry</strong>: GeoJSON geometry of the tile</li></ul><h2 id="probe-data-model" tabindex="-1">Probe data model <a class="header-anchor" href="#probe-data-model" aria-label="Permalink to &quot;Probe data model&quot;">​</a></h2><p>The properties of a probe object are described by the following data model:</p><p><img src="'+l+'" alt="Probe data model"></p><p>This data model is instantiated when forecast data are asked to be probed by the <a href="./../api/probe.html">Probe plugin</a> and manipulated through the <a href="./../api/probe.html#probes-api">Probes API</a>.</p><p>The common model for on-demand or stream mode is a <a href="https://tools.ietf.org/html/rfc7946#section-3.3" target="_blank" rel="noreferrer">GeoJSON feature collection</a>, where each <a href="https://tools.ietf.org/html/rfc7946#section-3.2" target="_blank" rel="noreferrer">GeoJSON feature</a> describes a location of interest.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>For now only features with <a href="https://tools.ietf.org/html/rfc7946#section-3.1.2" target="_blank" rel="noreferrer">GeoJSON point geometry</a> are supported.</p></div><p>The following additional properties of the GeoJSON collection allows to configure the probe:</p><ul><li><strong>forecast</strong>: the forecast model instance name to be probed according to the <a href="./../guides/basics.html#configuring">configuration</a>,</li><li><strong>elements</strong>: array of names corresponding to the forecast element values to be probed in the forecast data (each element has to be declared in the the <a href="./../guides/basics.html#configuring">configuration</a>)</li><li><strong>featureId</strong>: only useful for streamed probes, indicates which field(s) can be used as unique identifier of your features (support dot notation)</li></ul><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Features of the collection are usually numerous so they are not returned by default, you have to explicitly ask for using <a href="https://docs.feathersjs.com/api/databases/querying.html#select" target="_blank" rel="noreferrer"><code>$select</code></a>.</p></div><h2 id="probe-result-data-model" tabindex="-1">Probe result data model <a class="header-anchor" href="#probe-result-data-model" aria-label="Permalink to &quot;Probe result data model&quot;">​</a></h2><p>The properties of a probe result object are described by the following data model:</p><p><img src="'+s+'" alt="Probe data model"></p><p>This data model is instantiated when forecast data are probed by the <a href="./../api/probe.html">Probe plugin</a> and manipulated through the <a href="./../api/probe.html#probe-results-api">Probe results API</a>.</p><p>The common model for probe results is a <a href="https://tools.ietf.org/html/rfc7946#section-3.2" target="_blank" rel="noreferrer">GeoJSON feature</a> with <a href="https://tools.ietf.org/html/rfc7946#section-3.1.2" target="_blank" rel="noreferrer">GeoJSON point geometry</a>. Each result contains an additional property per probed element (in the <code>properties</code> field), according to its <a href="./../guides/basics.html#configuring">configured name</a>, storing the target element value. It also contains the following:</p><ul><li><strong>runTime</strong>: the date/time of the model run that produced the forecast data that has been used to produce the probe result</li><li><strong>forecastTime</strong> : the date/time of the forecast data that has been used to produce the probe result</li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>When performing probe results aggregation probed value, runTime and forecastTime will be arrays ordered according to ascending time and stored in an additional property per probed element according to its <a href="./../guides/basics.html#configuring">configured name</a></p></div><h2 id="alert-data-model" tabindex="-1">Alert data model <a class="header-anchor" href="#alert-data-model" aria-label="Permalink to &quot;Alert data model&quot;">​</a></h2><p>The properties of an alert object are described by the following data model:</p><p><img src="'+n+'" alt="Alert data model"></p><p>This data model is instantiated and manipulated through the <a href="./../api/alert.html">Alert API</a>.</p><p>The details of each property are the following:</p><ul><li><strong>cron</strong>: a <a href="https://github.com/kelektiv/node-cron" target="_blank" rel="noreferrer">CRON pattern</a> to schedule the alert check at regular intervals, e.g. <code>* 0 * * * *</code> will run it every minute</li><li><strong>probeId</strong>: the ID of the probe to be checked by the alert</li><li><strong>elements</strong>: array of names corresponding to the probed forecast element values to be checked by the alert (each element has to be declared in the <a href="./../guides/basics.html#configuring">configuration</a>)</li><li><strong>featureId</strong>: indicates which field(s) can be used as a unique identifier of your probe features (support dot notation)</li><li><strong>period</strong>: time interval used to do the matching between the forecast data and the trigger conditions <ul><li><strong>start</strong>: offset which needs to be added to the current timestamp at the moment of validation to compute the beginning of the time interval used to check the conditions, specified as <a href="https://momentjs.com/docs/#/manipulating/add/" target="_blank" rel="noreferrer">moment.js duration object</a></li><li><strong>end</strong>: offset which needs to be added to the current timestamp at the moment of validation to compute the end of the time interval used to check the conditions, specified as <a href="https://momentjs.com/docs/#/manipulating/add/" target="_blank" rel="noreferrer">moment.js duration object</a></li></ul></li><li><strong>conditions</strong>: query specification which contains nested objects: <ul><li><strong>[element]</strong>: for each checked element a filter according to the computed element values specified using the <a href="https://docs.feathersjs.com/api/databases/querying.html" target="_blank" rel="noreferrer">Feathers common database query API</a></li><li><strong>geometry</strong> specification of a <a href="https://docs.mongodb.com/manual/reference/operator/query/geoWithin/" target="_blank" rel="noreferrer">geospatial query</a></li></ul></li></ul>',42),h=[c];function g(m,f,u,p,b,y){return o(),t("div",null,h)}const _=e(d,[["render",g]]);export{v as __pageData,_ as default};
