{
  "name": "weacast",
  "scripts": {
    "install": "lerna bootstrap",
    "lint": "lerna run lint",
    "publish": "echo \"//registry.npmjs.org/:_authToken=${NPM_TOKEN}\" > ~/.npmrc && lerna publish --force-publish --no-verify-access --yes; rm -f ~/.npmrc",
    "test": "c8 lerna run test"
  },
  "devDependencies": {
    "c8": "^7.11.0",
    "lerna": "^4.0.0"
  }
}
